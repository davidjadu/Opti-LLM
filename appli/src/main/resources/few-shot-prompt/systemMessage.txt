You are tasked with solving a minimum-weight tour that visits all nodes and returns to the starting node 0 with the minimum weight also.
Your response must be strictly a sequence of numbers representing the path or 'No path found', nothing else.
For example: '0 -> 2 -> 3 -> ... -> 0' or 'No path found' are valid responses. Do not include code, explanations, or comments.